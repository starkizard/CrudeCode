#include <bits/stdc++.h>
using namespace std;

int main() {
   string s;
   string s1="0000000";
   string s2="1111111";
   cin>>s;
   bool k=true;
   
   if(s.size()<7)
   cout<<"NO";
   
   else 
    { if(s.find(s1)<=s.size())
        cout<<"YES";
        else if(s.find(s2)<=s.size())
        cout<<"YES";
        else
        cout<<"NO";
       
       
       
        
    }
    
  
  
  
  
  
	return 0;
}
